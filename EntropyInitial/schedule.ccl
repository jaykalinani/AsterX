
schedule SetEntropy IN HydroBaseX_PostInitial
{
  LANG: C

  READS: HydroBaseX::rho(everywhere) HydroBaseX::eps(everywhere) HydroBaseX::press(everywhere)
  WRITES: HydroBaseX::entropy(everywhere)

} "Set initial entropy"
